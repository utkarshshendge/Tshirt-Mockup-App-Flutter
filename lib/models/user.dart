class User {
   String uId;
   String profileName;
   String username;
   String bio;
   String email;
  User({this.profileName, this.username, this.bio, this.email, this.uId});

  factory User.data( ) {
    return User(

      
    );
  }
}
